spring:
  application:
    name: CourseCenter

  security:
    user:
      name: aks
      password: 123

  datasource:
    url: jdbc:mysql://localhost:3306/CourseCenter
    username: root
    password: 502250

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true
